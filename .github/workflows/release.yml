name: Release QGIS Plugin

on:
  push:
    tags:
      - '*'

jobs:
  build-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install zip
        run: sudo apt-get install zip -y

      - name: Build plugin zip
        run: |
          mkdir -p IGN_assistant-route
          rsync -av --progress . IGN_assistant-route/ \
            --exclude .git \
            --exclude .github \
            --exclude IGN_assistant-route
          zip -r IGN_assistant-route.zip IGN_assistant-route

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "IGN_assistant-route.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}